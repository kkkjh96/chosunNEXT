<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chosunnext.dao.CodeDao">

    <select id="getCategories" resultType="com.example.chosunnext.dto.category.response.ResponseCategoryDto">
        SELECT *
            FROM tbl_codeinfo
            WHERE parent_cd = 'MAIN_CATEGORY_CD'  -- parent_cd가 main_category_cd인 항목
        UNION
        SELECT *
            FROM tbl_codeinfo
            WHERE parent_cd IN (
                SELECT cd
                FROM tbl_codeinfo
                WHERE parent_cd = 'MAIN_CATEGORY_CD'  -- main_category_cd의 하위 소분류 조건
            )
        ORDER BY parent_cd
    </select>

</mapper>
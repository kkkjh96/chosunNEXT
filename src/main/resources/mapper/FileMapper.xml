<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chosunnext.dao.FileDao">

    <insert id="uploadsFile">
        INSERT INTO (file_gbn_cd, file_ref_id, file_name, file_url, inst_id, inst_dt)
        VALUES
        (#{fileGbnCd}, #{fileRefId}, #{fileName}, #{fileUrl}, #{instId}, NOW());
    </insert>

</mapper>